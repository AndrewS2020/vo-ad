TELEGRAM_BOT_TOKEN=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
# City the address registry (data/addr.csv) covers. Swapping cities means
# swapping the registry data too, not just this value.
CITY=Київ
